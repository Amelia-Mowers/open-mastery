id: prealg.lineq.divide.f01
skills: [prealg.lineq.divide]
difficulty: 1
representation: balance-scale
params: { a: 5, b: 40, variable: x }
widget: { type: equation-input, config: { stem: "Finish solving: {a}{variable} = {b}." } }
answer: { type: expr, value: "{variable} = {b/a}", equivalence: symbolic, integer: true }
hints:
  - "Step 2 already divided both sides by {a}."
faded:
  reveal_steps: [1, 2]
  student_completes: [3]
  steps:
    - "{a}{variable} = {b}"
    - "Divide both sides by {a}:  {a}{variable} ÷ {a} = {b} ÷ {a}"
    - "{variable} = ?"
source: { book: openstax-prealgebra-2e, section: "8.2" }
verify: "{a * answer == b}"
review: { status: draft, by: "@claude-fable-5", date: 2026-08-26 }
